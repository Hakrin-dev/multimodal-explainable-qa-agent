# 多模态数据驱动的可解释精准问数/问答智能体 —— 项目方案（Plan v2 · 已定案）

> 对应赛题：“中国电子杯”第三届高校 ICT 产教融合创新大赛 · 赛题八（中电云计算技术有限公司）
> v2 变更：落实 D1~D11 全部决策；新增 LLM 选型评测方案（§10）、预算管控与缓存设计（§11）、团队分工（§12）；按实际时间线（2026-09-21 ~ 2026-10-24，5 周含国庆）重排里程碑（§13）。

---

## 1. 赛题解读与得分策略

### 1.1 赛题本质

构建一个统一智能体，同时具备：
- **问数（NL2SQL）**：自然语言 → 结构化数据库查询（精准性是生命线）
- **问答（RAG）**：自然语言 → 非结构化知识库检索与生成（忠实度是生命线）
- **跨源多跳推理**：如“查 2024 销售冠军的业绩 + 总结他的成功方法论”（先查库、再搜文档）
- **可解释性**：全程展示推理链路、工具调用、SQL、引用溯源（贯穿两阶段评分的独立得分项）
- **鲁棒性与交互**：错别字/别名/模糊输入容错、信息缺失主动澄清、多轮上下文

### 1.2 得分拆解与目标（初赛 100 分）

| 评分项 | 分值 | 我们的策略 | 目标 |
|---|---|---|---|
| 基础 NL2SQL 单表（10 用例） | 10 | 自建 Chinook 测试集，稳拿 | 10 |
| 基础 RAG 单文档（10 用例） | 10 | 自建知识库 + 引用机制 | 10 |
| 答案可解释性 | 5 | 推理链路 DAG + SQL/引用/改写对照 | 5 |
| 交互 Demo 可用性 | 5 | Vue 3 流式对话界面 | 5 |
| NL2SQL 执行准确率 | 10 | 自修复循环 + Schema Linking | 10（≥90%） |
| RAG 忠实度 | 10 | 混合检索+重排+引用自检 | 8~10 |
| 系统效率分析（文档） | 5 | 多规模实测+复杂度分析+缓存优化叙事 | 5 |
| 中级任务攻克 | 15 | 初赛攻 5 个（#1+#2、#4、#5、#9） | 15+ |
| 系统鲁棒性 | 10 | 别名/纠错/低质量文档三线防御 | 7~10 |
| 多轮对话 | 5 | 指代消解改写 + 会话记忆 | 5 |
| 文档质量 | 10 | 按评分模板撰写，工业风格，W3 起骨架并行 | 8~10 |
| 演示效果 | 5 | 10 个演示场景脚本 + 视频 | 5 |

**初赛关键结论**：
1. 基础 40 分必须 100% 拿满，靠自建测试集保证（评委用我们的测试集跑）。
2. 中级任务“每攻克 1 个得 5 分、上限 15 分”，初赛攻 5 个（3 个拿满基础分，多余的换“质量突出”加分与决赛储备）。
3. 鲁棒性 10 分与中级任务 #1（要素识别与改写）强耦合，一起设计。
4. 多轮 5 分便宜，必拿。
5. **时间约束下（5 周），#7 目录鲁棒识别在初赛做“简化版”（正样本正常处理 + 1 个坏文档演示），完整方案放决赛**。

### 1.3 得分拆解与目标（决赛 100 + 20 加分）

| 评分项 | 分值 | 策略 |
|---|---|---|
| 多源异构融合问数/问答 | 10 | 跨源多跳 Agent 规划，覆盖 ≥5 种跨源问题类型 |
| 智能意图理解与澄清 | 10 | 中级任务 #3 深化：意图分类 + 槽位校验 + 选项式澄清 |
| 答案可解释性 | 5 | DAG 可视化 + PDF 原文定位高亮 |
| NL2SQL 复杂查询准确率 | 10 | 中级任务 #4 + #5，测试集覆盖 JOIN/聚合/嵌套/HAVING/窗口 |
| RAG 多文档/多模态问答 | 10 | 多文档检索 + 公式计算 + 扫描件 OCR 链路 |
| 效率与伸缩性证明 | 5 | 3 种数据规模实测 + 吞吐量 + 瓶颈分析 |
| 低质量数据鲁棒性 | 5 | 中级任务 #9 + OHR-Bench 实证 |
| 中级任务广度（5 个以上满 15） | 15 | 决赛覆盖 7~8 个 |
| 中级任务质量/独创性 | 10 | 每个任务给出“基线 vs 我们”的量化对比 |
| 文档/答辩/演示/综合 | 20 | 常规保障 |
| 高级任务加分（自选申报） | 0~20 | “实用创新”方向（见 1.4） |

### 1.4 高级任务加分（决赛，已定方向 A）

申报**实用创新与转化价值**：自研轻量 Agent 编排内核（事件驱动、全链路 Trace 原生）+ “检索式要素中台”（业务知识从 Prompt 剥离到可检索资产库，新领域零改码接入）+ 分级评测与缓存前缀的成本工程（¥1000 预算内完成全量研发与评测，本身就是工程化叙事素材）。OHR-Bench 实证作为举证材料。

---

## 2. 系统总体架构

```
┌──────────────────────────── 前端 Vue 3 + TS ────────────────────────────┐
│  对话流（SSE） │ 推理链路 DAG │ 数据卡片(表格/图表) │ 引用溯源(PDF定位) │
│  澄清交互组件  │ 文档管理台(上传→质量报告→修复) │ 会话管理 │ 设置          │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │ REST + SSE
┌────────────────────────────────▼────────────────────────────────────────┐
│                     后端服务 FastAPI（Python 3.11+）                     │
│                                                                          │
│   会话与记忆模块              Agent 编排引擎（自研轻量内核）                │
│   · 多轮历史 / 指代消解       · 意图分类 → 任务规划(DAG) → 工具调度       │
│   · 澄清状态机               · 失败重试 / 超时控制 / Trace 全程记录       │
│                                                                          │
│   ┌─────────────────────┐  ┌──────────────────────┐  ┌───────────────┐  │
│   │  NL2SQL 引擎         │  │  RAG 问答引擎          │  │ 公式计算引擎    │  │
│   │ 问题改写             │  │ 混合检索(向量+BM25)    │  │ LaTeX→SymPy   │  │
│   │ 要素识别与标准化      │  │ Rerank 精排           │  │ 参数槽位绑定   │  │
│   │ Schema Linking       │  │ 层级感知上下文组装     │  │ 双通道校验     │  │
│   │ Join 路径推理        │  │ 引用生成+忠实度自检    │  │               │  │
│   │ SQL 生成/校验/自修复  │  │                      │  │               │  │
│   └─────────┬───────────┘  └──────────┬───────────┘  └───────┬───────┘  │
│             │                         │                      │          │
│   ┌─────────▼─────────────────────────▼──────────────────────▼───────┐  │
│   │                    可解释性中心（Trace 数据模型）                  │  │
│   │         LLM 抽象层（provider 切换 / 响应缓存 / 前缀缓存友好）      │  │
│   └──────────────────────────────────────────────────────────────────┘  │
└──────┬──────────────────────────┬───────────────────────────┬───────────┘
       ▼                          ▼                           ▼
┌───────────────┐        ┌─────────────────┐        ┌────────────────────┐
│ PostgreSQL    │        │ 文档摄入流水线    │        │ 本地模型服务(GPU)   │
│ · Chinook     │        │ 质量评估→复杂度   │        │ · MinerU(主)       │
│   (业务库)     │        │ 评估→解析路由→   │        │ · BGE-M3 + rerank  │
│ · 业务术语库   │        │ 目录重建→切片→   │        │ · SiliconFlow API  │
│ · Schema 索引 │        │ 公式资产登记→    │        │   (配置化备胎)      │
│ · pgvector    │        │ 混合索引         │        │ · PaddleOCR(CPU)   │
│ · 会话/Trace  │        │                 │        │                    │
└───────────────┘        └─────────────────┘        └────────────────────┘
```

**架构三原则**：
1. **知识外置**：业务别名、schema 语义、公式全部沉淀为“可检索资产库”，Prompt 只含通用能力 → 天然攻克中级任务 #2（可扩展要素提取）。
2. **全链路 Trace**：编排内核每个节点（意图/规划/每个工具调用/融合）产出统一事件模型 → 可解释性评分与调试效率双收。
3. **校验优先**：SQL 必过 sqlglot AST 校验 + 试执行 + 自修复循环；答案必过引用支撑自检 → 精准性与忠实度两大性能分。
4. **成本工程**（v2 新增）：LLM 抽象层统一 provider 切换、响应缓存、前缀缓存友好模板（见 §11）。

---

## 3. 技术栈选型（已定案）

| 层 | 决议 | 说明 |
|---|---|---|
| 后端 | Python 3.11 + FastAPI | SSE 流式 |
| 前端 | Vue 3 + TS + Vite + **Naive UI** | D11 决议 |
| 图可视化 | AntV X6（推理 DAG）+ ECharts（数据图表） | |
| 业务数据库 | **Chinook PG 版 + pgvector**（初赛）；决赛加 AdventureWorks (chriseaton/adventureworks:postgres) | D2-C 决议，分阶段 |
| 向量库 | pgvector（初赛）；决赛可升级 Qdrant 讲伸缩性 | D4 决议 |
| 主力 LLM | **W1 评测定**（DeepSeek-V3 vs Qwen-Plus，10 NL2SQL + 5 RAG 用例，半天，见 §10）；备胎配置文件切换，不做自动 failover | D1 决议 |
| Embedding / Rerank | **本地部署**（BGE-M3 + bge-reranker-v2-m3，GPU）+ SiliconFlow API 配置化备胎 | D9-B 决议 |
| 文档解析 | **MinerU 本地 GPU pipeline 为主** + SiliconFlow API 配置化备胎；PaddleOCR CPU 兜底；原生 PDF 用 PyMuPDF 直抽 | D5 决议 |
| SQL 校验 | sqlglot | |
| 公式计算 | SymPy + LLM LaTeX→SymPy 转换 | |
| Agent 编排 | **自研轻量内核**（事件驱动状态机） | D3-A 决议 |
| 部署 | docker-compose 一键启动 + quick_start.sh | 赛题要求 |

---

## 4. 核心模块详细设计

### 4.1 Agent 编排引擎（自研轻量内核）

**意图分类**（LLM 结构化输出，一次调用）：
- `CHAT` 闲聊 → 直接生成
- `DB_QUERY` 问数 → 走 NL2SQL 流水线
- `DOC_QUERY` 问答 → 走 RAG 流水线
- `HYBRID` 跨源 → 规划器分解为子任务 DAG
- `AMBIGUOUS` 信息缺失 → 澄清流程（中级任务 #3）

**规划器**：HYBRID 问题分解为依赖子图。例：“2024 销售冠军是谁？总结他的方法论”
→ `[T1: DB查询-年度销售冠军] → [T2: RAG检索-该员工经验/方法论文档] → [T3: 融合生成]`

**工具集**：
- `nl2sql(question)` → {sql, rows, columns, chart_hint}
- `rag_search(question, filters?)` → {chunks[{doc,page,breadcrumb,text,score}]}
- `formula_eval(formula_id, params)` → {value, steps}（中级任务 #6）
- `ask_user(clarification, options)` → 挂起等待用户输入
- `db_lookup_entity(term)` → 术语库反查（要素标准化）

**可靠性机制**：每工具调用带超时与重试预算；SQL 失败走自修复子循环；DAG 节点失败可降级（跨源问题中文档检索为空时明确说明“未在知识库中找到”，而非编造）。

**Trace 事件模型**（可解释性核心数据结构）：
```
TraceNode { id, parent_id, type: intent|plan|tool_call|llm_call|fuse|clarify,
            label, input, output, latency_ms, status, children[] }
```
每轮问答产出一棵 Trace 树，前端渲染为时间线 + DAG 双视图。

### 4.2 NL2SQL 引擎（流水线）

```
原始问题 ──► ① 多轮改写(指代消解/省略补全/错别字纠正)
        ──► ② 要素识别与标准化(术语库检索式链接)      ←中级#1/#2
        ──► ③ Schema Linking(表级+列级双粒度召回)      ←中级#4
        ──► ④ Join 路径推理(外键图+LLM选择)            ←中级#5
        ──► ⑤ SQL 生成(先分析草稿后SQL，双步输出)
        ──► ⑥ 校验与自修复(sqlglot AST→白名单→EXPLAIN→试执行
                            →错误反馈重试≤3次→空结果归因)
        ──► ⑦ 结果解释(自然语言总结+图表建议)
```

**② 要素标准化——检索式术语库（中级 #1+#2 合并攻克）**：
- 术语库表结构：`term(canonical_name, aliases[], type[实体/指标/维度/单位], binding[table.column | metric_id], description, embedding)`
- 数据来源：a) schema 枚举列 distinct 值自动抽取（Chinook 的 Genre、City、Employee 名等）；b) 人工种子别名（“帝都”→北京、“营收”→Total、“销量”→Quantity）；c) LLM 批量生成候选别名 + 人工审核入库
- 在线匹配：问题 n-gram + NER 候选 span → 向量召回 + 字面相似度（编辑距离，处理错别字）→ 置信度阈值内替换为 canonical 并记录改写对照（可解释素材）
- **扩展新领域 = 只跑一次抽取脚本 + 审核界面，Prompt 零改动** → 这就是“不依赖 Few-Shot”的可扩展方案

**③ Schema Linking（中级 #4）**：
- 离线：每表生成语义卡（表名+业务描述+列注释+主外键+每列 top 示例值+行数），表级与列级分别建索引
- 在线：标准化问题 → 向量+BM25 双路召回 top-k 表 → LLM 精排过滤干扰表（如 Chinook 中 Invoice vs InvoiceLine）→ 输出压缩 schema（仅相关表+必带关联表）

**④⑤ SQL 生成**：
- Prompt 结构：`[压缩schema DDL+注释+枚举值样例] + [显式 Join 路径(外键图搜索结果)] + [标准化后问题] + [约束：只读/JOIN须用给定路径/聚合显式GROUP BY]`
- 双步输出：先输出分析草稿（目标表、过滤条件、聚合维度），再输出 SQL —— 提升稳定性且草稿直接进 Trace 展示

**⑥ 自修复循环**：
1. sqlglot 解析：语法错误定位
2. 白名单校验：仅 SELECT；表/列存在性；无危险函数
3. EXPLAIN 试跑：成本异常（全表扫描+笛卡尔积特征）预警
4. 执行：异常信息+原 SQL 回喂 LLM 修复（≤3 次）
5. 空结果归因：区分“真无数据”（下推 `SELECT count(*)` 佐证并如实回答）与“查询可疑”（条件过严/Join 错路径 → 触发复查或澄清）

### 4.3 RAG 问答引擎

**摄入流水线（离线）**：
```
文件 → ① 质量评估(方向/清晰度/文本层有无/繁简/错别字)   ←中级#9
     → ② 复杂度评估(1-5级) → 解析策略路由                ←中级#8(轻量)
     → ③ 解析：原生PDF(PyMuPDF直抽) / 扫描件(MinerU本地GPU / PaddleOCR CPU兜底)
     → ④ 目录结构重建(信号融合+LLM辅助层级判定)           ←中级#7(初赛简化版)
     → ⑤ 层级感知切片(256~1024 token, 块带面包屑 H1>H2>H3)
     → ⑥ 公式资产登记(LaTeX+参数上下文说明)               ←中级#6(决赛为主)
     → ⑦ 混合索引(pgvector 稠密 + BM25) + 元数据(doc/page/面包屑/块类型/质量分)
```

**在线检索问答**：
- 查询改写（多轮融合 + 可选 HyDE）→ 混合检索（向量 + BM25，RRF 融合）→ Rerank 精排 top-k → 层级感知上下文组装（带面包屑前缀）→ 生成（内联引用 [1][2]）→ **忠实度自检**（LLM 校验每个答案句是否有引用支撑，不支撑则收敛重写）

**引用溯源**：答案中引用可点击 → 前端打开对应 PDF 页面并高亮片段（PDF.js）→ 直接命中“展示质量高得 5 分”的可解释性档位。

### 4.4 公式计算引擎（中级 #6，两问分治；初赛做最小闭环，决赛深化）

- **6a 参数识别**：摄入期公式块登记时，LLM 抽取公式上下文中的参数说明（变量名→语义标签）；问答期对“计算型问题”触发参数槽位抽取——参数值来源三通道：文档内数值 / 用户输入 / **数据库查询结果**（例：“按员工手册提成公式，Jane 的本月提成”→ 公式来自文档，销售额来自 DB）
- **6b 公式结构解析与计算**：LaTeX → SymPy 表达式（LLM 转换 + 编译校验）→ 参数绑定代入 → 双通道校验（SymPy 数值重算 + LLM 估算数量级比对）→ 计算步骤进 Trace 展示

### 4.5 跨源融合与多跳（决赛 10 分主攻；初赛打通 2 种类型保演示）

- 覆盖问题类型（目标 ≥5 种）：① DB→Doc 实体档案型（冠军方法论）；② Doc→DB 参数查询型（文档公式+DB 数据计算）；③ Doc→Doc 多文档对比；④ DB 结果 + Doc 背景解释综合型；⑤ 澄清后跨源续查型
- 融合生成器：输入 = 各子任务结构化结果 + 引用，输出带分区溯源的答案（“数据部分来自数据库查询，方法论部分来自《销售手册》P12”）

### 4.6 记忆与多轮

- 会话存储（PG 表）：消息、Trace、澄清挂起状态
- 每轮先做**指代消解改写**（“他呢？”→“Jane 的上级是谁”），改写对照在 UI 展示
- 目标：脚本化 5~8 轮连续对话测试（含改写、追问、话题切换、澄清恢复四种模式）

### 4.7 澄清机制（中级 #3，决赛 10 分主攻；初赛做基础版保演示）

- **必要槽位矩阵**（外置配置，非硬编码）：按问题类型定义必需要素（增长率→基准期+对比期；TopN→N；对比→双方实体；趋势→时间窗）
- LLM 结构化输出：`{intent, slots, missing_slots[], candidate_options{}}`
- 交互设计：选项按钮式澄清（点选而非打字）→ 澄清结果回填槽位 → 自动续查
- 评测：构造 30 个缺信息问题，澄清触发准确率 + 澄清后任务完成率

---

## 5. 中级任务攻克方案总表

| # | 任务 | 方案核心 | 量化指标（写进技术文档） | 初赛范围 |
|---|---|---|---|---|
| 1 | 要素识别与问题改写 | 检索式术语库 + 向量/字面双路实体链接 | 别名/错别字变体集识别准确率；改写前后 NL2SQL 成功率对比 | ✅ 完整 |
| 2 | 可扩展要素提取 | 同上（知识外置，Prompt 零 Few-Shot）；新领域接入演示 | 新 schema 接入耗时（分钟级）；要素命中率 | ✅ 完整 |
| 3 | 智能语义校验与澄清 | 槽位矩阵 + 选项式澄清 + 状态回填续查 | 30 个缺信息问题：触发准确率 / 澄清后完成率 | ⚪ 基础版（单轮澄清闭环） |
| 4 | 精准表定位 Schema Linking | 表级+列级双粒度索引、LLM 精排 | 表召回@k、列定位准确率、干扰表排除率 | ✅ 完整 |
| 5 | 高质量多表关联 NL2SQL | 外键图 Join 路径搜索 + 显式路径注入 + 自修复循环 | 多表用例执行准确率（基线 vs 加路径注入消融） | ✅ 完整 |
| 6 | 文档公式识别与计算 | 公式资产登记 + 参数槽位三通道绑定 + SymPy + 双通道校验 | 公式计算正确率（构造含公式文档集） | ⚪ 最小闭环（1-2 个公式演示） |
| 7 | 不标准目录鲁棒识别 | 字体/编号/目录页/位置信号融合 + LLM 层级判定 | 自造乱目录 PDF 集上标题层级 F1；对 RAG 命中率提升 | ⚪ 简化版（正常目录 + 1 个坏文档） |
| 8 | 文档复杂度评估 | 多特征加权评分（可解释规则）→ 1-5 级 → 解析策略路由 | 30+ 文档人工标注一致率 | ⚪ 轻量（规则评分 + 路由） |
| 9 | 文档质量综合评估 | 方向/倾斜/清晰度/繁简/错别字检测 + 自动修复 + OHR-Bench 实证 | 缺陷检测准确率；修复后 RAG 指标提升幅度 | ✅ 完整 |

**初赛主打（计分 5 个）**：#1、#2、#4、#5、#9；#3/#6/#7/#8 做演示级基础版（不写入“攻克清单”，但为决赛铺路）。
**决赛扩展**：#3、#6、#7、#8 全部完整化 + AdventureWorks 规模化 + OHR-Bench 全量实证。

---

## 6. 数据资产与测试集（成败关键，评委用我们的用例跑分）

### 6.1 知识库文档（≥10 份，与 Chinook 数据库语义联动）

**核心设计**：围绕 Chinook“数字媒体商店”虚构一家公司（如 "Chinook 唱片"），知识库与 DB 实体强关联，跨源多跳才成立：

| 文档 | 类型/缺陷设计 | 服务场景 |
|---|---|---|
| 员工手册 | 含**提成计算公式**、考勤制度 | 公式计算 #6、基础 RAG |
| 销售团队年度总结（各员工方法论） | 与 Employee 表对应 | 跨源：“冠军的方法论” |
| 产品线/曲风运营说明 | 与 Genre 表联动 | 跨源参数查询 |
| 客户服务 SOP | 规章制度类 | 基础 RAG、多文档 |
| 供应商合作协议 | 含表格、条款 | 表格问答 |
| 营销活动复盘报告 | 多级目录正常 | 目录识别正样本 |
| **故意做坏的文档 ×3**：目录层级丢失、页面颠倒扫描件、模糊+繁体混排 | 鲁棒性 | #7/#9 演示 |
| 财务制度（含多公式） | 公式密集 | #6 压力测试 |

生成方式：LLM 批量起草 + 人工校对 → 导出 PDF（部分转扫描件：渲染图片+旋转+噪声+压缩，程序化制造缺陷，脚本一次性投入）。
补充：OHR-Bench 子集作为 #9 的独立实证数据（官方推荐数据集）。

### 6.2 测试集（写进 eval/，附自动跑分脚本）

- `NL2SQL 单表`：20 用例（覆盖过滤/排序/聚合/LIKE/IN/日期）
- `NL2SQL 多表`：20 用例（2~4 表 JOIN、聚合、嵌套、HAVING）
- `RAG 单文档`：20 用例（事实型/数值型/条款型）
- `RAG 多文档`：15 用例（跨文档对比/综合）
- `跨源多跳`：10 用例（覆盖 5 种类型）
- `鲁棒性变体`：上述用例的错别字/别名/口语化变体 ×30
- `澄清`：30 个缺信息问题
- `多轮`：8 个 5+ 轮脚本场景
- 评测方法：NL2SQL 用执行结果集匹配（execution accuracy）；RAG 用关键事实匹配 + LLM-as-judge 双评；全部可一键回归（分级见 §11.2）。

---

## 7. 系统效率与伸缩性证明（评分项要求的内容提前规划）

- 理论分析：各模块复杂度（检索 O(logN)、Schema Linking 召回随表数线性、LLM 调用次数上界等）
- 实测矩阵：文档规模 10/100/1000 篇 × 表规模（Chinook 11 表 vs AdventureWorks 71 表）的 P50/P95 延迟；吞吐量（并发 1/4/8）
- 优化叙事（**有实测支撑**）：schema 压缩降低 token 成本、三层缓存（提供商前缀缓存 / 应用层响应缓存 / embedding 增量缓存）的命中率与成本对比、流式输出首字延迟、（决赛）Qdrant 分片伸缩
- 全部由 eval 脚本自动产出图表 → 直接进技术文档

---

## 8. 项目结构与部署（规划）

```
multimodal-qa-agent/
├── backend/app/{api, agent, nl2sql, rag, formula, ingestion, explain, memory, core}
│   core/llm.py            # LLM 抽象层：provider 配置切换 / 响应缓存 / 用量记账
│   core/prompts/          # 三层 Prompt 模板（静态/半静态/动态，见 §11.3）
├── backend/eval/            # 测试集 + 分级跑分脚本（L0/L1/L2）
├── backend/tests/
├── frontend/                # Vue 3
├── data/{db_schema, docs_raw, docs_parsed, eval_cases}
├── deploy/docker-compose.yml + quick_start.sh
└── docs/{设计报告, 技术实现说明书}
```

docker-compose：postgres(pgvector) + backend + frontend；本地 GPU 服务（MinerU/BGE）以 sidecar 脚本启动，无 GPU 环境自动降级 SiliconFlow API（配置化，不改代码）。

## 9. 演示场景脚本（演示 5 分 + 答辩素材，10 个）

1. 单表问数：“销量前 10 的曲目” → SQL + 表格 + 图表
2. 单文档问答：“年假有几天” → 答案 + PDF 定位高亮
3. 别名/错别字：“帝都客户的订单” / 含错字问题 → 改写对照展示
4. 主动澄清：“增长率是多少” → 选项式澄清 → 续查
5. 多表关联：Top 客户消费明细 + 曲风分布
6. **跨源多跳**：“2024 销售冠军是谁？总结他的方法论” → DAG 展示先查库后搜文档
7. 公式计算：“按员工手册提成公式，Jane 本月提成多少” → 公式+参数+计算步骤
8. 多轮：5+ 轮连续追问（含指代）
9. 文档管理台上传坏文档 → 质量报告 → 修复前后对比 → 问答
10. 推理链路 DAG 全程回放

---

## 10. LLM 选型评测方案（W1 · 半天 · D1 决议）

**目标**：DeepSeek-V3 vs Qwen-Plus 数据说话定主力；评测脚本沉淀为后续回归资产。

- **用例**：10 个 NL2SQL（单表 7：过滤/排序/聚合/LIKE/日期；双表 JOIN 3）+ 5 个 RAG（2 事实型 + 1 数值型 + 1 条款型 + 1 跨块定位）
- **公平性**：两模型共享同一 Prompt 模板、同一 schema 描述、同一检索片段；温度等参数一致；每用例重复 3 次取均值
- **评测维度**：
  | 维度 | NL2SQL | RAG |
  |---|---|---|
  | 质量 | 执行准确率（结果集匹配）；**免修复首过率**（首条 SQL 即合法且正确） | 关键事实命中（自动）+ LLM-as-judge（交叉） |
  | 速度 | P50/P95 延迟 | 同左 |
  | 成本 | 每用例 token 用量 × 官方价折算 | 同左 |
- **裁决规则**：执行准确率优先；差距 <5% 时按成本（含缓存价）与延迟定
- **产出**：对比报告（进技术文档附录）+ `.env` 的 `LLM_PROVIDER` 定案；备胎模型保留配置项，不做自动 failover

---

## 11. 预算管控与缓存设计（D10 决议：API 预算 ≤ ¥1000）

### 11.1 预算分配（目标总花费 ≤ ¥800，留 ¥200 余量）

| 用途 | 预估 | 说明 |
|---|---|---|
| W1 选型评测 | ~¥10 | 15 用例 × 2 模型 × 3 次 ≈ 90 次调用 |
| 开发迭代（prompt 调试） | ~¥250 | 全程走 L0 smoke + 响应缓存 |
| 里程碑回归跑分 | ~¥150 | L1 每日 / L2 里程碑各一次 |
| 文档批量起草（知识库 10-15 份 + 别名生成） | ~¥50 | 一次性 |
| 演示录制 + 提交前全量验证 | ~¥100 | |
| 余量 | ~¥300 | 应对返工/换模型重跑 |

> Embedding / Rerank / MinerU / PaddleOCR 全部本地 GPU/CPU，**零 API 成本**；LLM 抽象层内置用量记账（每次调用落库：模型、tokens、折算费用），`eval` 可输出累计账单。

### 11.2 跑分脚本分级（D10 决议）

| 级别 | 范围 | 次数/耗时 | 成本 | 触发时机 |
|---|---|---|---|---|
| **L0 smoke** | 每类 5 用例，主力模型 | 单次，~2 min | <¥0.5 | 每次改 prompt / 改流水线代码后 |
| **L1 regression** | 全量 ~140 用例，主力模型 | 单次，~15 min | ~¥5-10 | 每日收工 / 功能合并 |
| **L2 full** | 全量 × 主备模型 × 3 重复 | ~45 min | ~¥30-50 | 里程碑节点 / 提交前 |

- 所有级别**默认走响应缓存**（§11.3），`--no-cache` 强制真调用；`--sample N` 随机抽样
- 结果落库（用例 id / 模型 / 得分 / diff），自动生成趋势图表 → 直接进技术文档

### 11.3 三层缓存设计（W2 落地进 Prompt 模板 · D10 决议）

**A. 提供商前缀缓存（Prompt 模板三层结构，W2 定稿）**：
```
[静态层] 角色 + 通用规则 + 输出格式规范 + SQL 生成守则   ← 跨请求不变，命中前缀缓存
[半静态层] schema 语义卡 / 术语样例 / 文档集说明          ← 同库/同文档集内稳定
[动态层] 用户问题 + 检索片段 + 多轮历史 + 澄清状态        ← 每次变化，永远放最后
```
- DeepSeek 前缀缓存命中价约为原价 1/10；Qwen 隐式上下文缓存同理 → **模板定稿后禁止随意改动静态层顺序**
- 收益可量化：`core/llm.py` 记录 cache hit token 数 → 技术文档给出“缓存前后成本对比曲线”（直接服务效率评分项 + 高级任务叙事）

**B. 应用层 LLM 响应缓存**：`hash(prompt + model + params + version)` → SQLite 落库；跑分重复执行直接复用（这是 L2 全量 × 3 重复成本可控的关键）；开发热路径秒回。

**C. Embedding / 解析增量缓存**：术语库与 schema 卡 embedding 入库时一次计算；文档解析/切片/公式登记结果持久化，重复摄入跳过（content hash 判重）。

---

## 12. 团队分工（3 人 · 每人 15~20 h/周）

| 角色 | 主责 | 交叉协作点 |
|---|---|---|
| **A：算法/编排** | Agent 编排内核 + Trace；NL2SQL 全流水线（#1/#2/#4/#5）；LLM 抽象层（provider/缓存/记账） | W1 末与 C 冻结 Trace 数据契约；W1 与 B 共同执行 LLM 选型评测 |
| **B：RAG/文档智能** | 摄入流水线（#9 完整 + #7 简化 + #8 轻量）；RAG 引擎（混合检索+重排+引用）；公式计算 #6 最小闭环；本地模型部署（MinerU GPU / BGE-M3 / reranker） | W1-W2 与 C 共建知识库文档与 RAG 测试集；W2 定摄入中间表示格式 |
| **C：全栈/评测/文档** | Vue 前端（对话流/DAG 可视化/PDF 溯源/文档管理台）；**评测体系 + 分级跑分脚本 + 用量记账报表**；技术文档牵头；演示脚本与视频 | W1 主导选型评测用例设计与执行；每周五发布节奏管理 |

- 交接物即接口：Trace JSON Schema、摄入中间表示（block/breadcrumb/formula 资产）、eval 用例格式 —— W1 末三份契约冻结后再并行开发
- 周节奏：周一 30min 计划会；周五 L1 回归跑分 + 15min 复盘；文档随代码走（每完成一个中级任务，当周产出该章节初稿）

---

## 13. 里程碑（2026-09-21 ~ 2026-10-24，5 周 · 含国庆）

> 总投入 ≈ 225~300 人时。**国庆周（10/1-10/7）按 50% 投入预估**，重活前移。

### W1（9/21-9/27）：地基 + 选型 + 最小闭环
- 环境：docker-compose（PG+pgvector+backend+frontend 骨架）、quick_start.sh
- 数据：Chinook PG 版导入；术语库抽取脚本 v1（枚举值自动抽取）；知识库文档起草启动（LLM 起草 + 人工校对流水线，B 主导）
- **LLM 选型评测（D1，半天）**：10 NL2SQL + 5 RAG × 2 模型 × 3 次 → 报告 + 主力定案
- 端到端最小闭环：单表 NL2SQL（改写→生成→sqlglot 校验→执行）+ 单文档 RAG（PyMuPDF 直抽→切片→向量检索→生成）各跑通 3 个示例
- 契约冻结：Trace JSON Schema、摄入中间表示、eval 用例格式
- **产出**：选型报告；最小闭环 demo 截图；三份契约文档

### W2（9/28-10/4）：编排 + 前端骨架 + 缓存落地（国庆前半周满负荷）
- A：编排内核（意图分类/工具调度/Trace 记录）+ LLM 抽象层（provider 切换/响应缓存/用量记账）
- **Prompt 三层模板定稿（静态层冻结，此后只追加不改序）——D10 决议的 W2 交付**
- C：前端对话流（SSE）+ SQL/引用展示 + 多轮会话；B：摄入流水线 v1（质量评估 #9 检测器 + 复杂度评分 #8 轻量 + MinerU 接入 + 层级切片）
- 知识库文档 v1 完成（10 份，含 3 份坏文档）
- **产出**：可演示的对话流 demo；缓存命中率首次统计

### W3（10/5-10/11）：中级任务主攻（国庆周，50% 投入，任务前移消化）
- A：#4 Schema Linking（表级+列级索引+LLM 精排）+ #5 Join 路径注入 + 自修复循环；#1/#2 术语库在线匹配全链路
- B：混合检索（BGE-M3 + BM25 + RRF）+ reranker 部署 + 引用溯源链路
- C：评测体系 v1（分级脚本 L0/L1 + 用例集 60%）+ 技术文档骨架（设计报告 + 说明书目录）
- **产出**：NL2SQL 多表用例首过 L1；RAG 引用可点击跳转 PDF

### W4（10/12-10/18）：跨源 + 澄清 + 鲁棒性闭环
- A：跨源多跳（规划器 + 2 种类型演示级）+ 澄清基础版（槽位矩阵 + 选项式交互 + 回填续查）
- B：#9 完整（自动修复 + 前后对比报告）+ #7 简化版（坏文档目录演示）+ 公式计算最小闭环（1-2 个公式）
- C：DAG 可视化（AntV X6）+ 文档管理台 + 用例集 100%（140+）
- **产出**：10 个演示场景中 8 个可跑；L2 首次全量回归

### W5（10/19-10/24）：打磨 + 文档 + 提交（最后 6 天，含 buffer）
- 全量回归（L2 × 3 重复）+ 瓶颈修复；效率实测矩阵（3 种文档规模 × 2 种表规模）出图
- 演示视频录制（10 场景，10 分钟内）；打包：源码 + schema 定义 + 知识库样本（≥10 份）+ 环境说明 + quick_start
- 技术文档定稿：设计报告（≤20 页，含“核心亮点预览页”）+ 技术实现说明书（≤30 页）
- **10/22 全员验收走查 → 10/23 冻结 → 10/24 提交**

### 裁剪预案（若 W3 结束进度 <80%）
1. 优先级保：基础 40 分 + #1/#2/#4/#5/#9 + 多轮 + 可解释性 DAG
2. 降级项：公式计算降为文档静态展示；跨源只保 1 种类型（DB→Doc）
3. 砍掉项：#7 简化版并入 #9 演示；效率实测降为 2 种规模

**风险与应对**：
- LLM SQL 生成不稳定 → 自修复循环 + 测试集驱动逐例修复 + 备胎模型切换
- MinerU GPU 环境问题 → SiliconFlow API 备胎已配置化（D5 决议），当天可切
- 国庆投入不足 → 重活已前移 W1-W2；W3 任务量按 50% 排
- 文档工程量超预期 → 坏文档程序化生成；正文 LLM 起草；校对分摊三人
- API 预算超支 → 用量记账日报监控；超 60% 预算即全面转 L0+缓存模式

---

## 14. 决策记录（D1~D11 已全部定案）

| # | 决策点 | 决议 |
|---|---|---|
| D1 | 主力 LLM | **W1 小型对比评测定**（DeepSeek-V3 vs Qwen-Plus，10 NL2SQL + 5 RAG 用例，半天，数据说话）；备胎配置文件切换，不做自动 failover |
| D2 | 业务数据库 | Chinook PG 版（初赛）+ 决赛加 AdventureWorks |
| D3 | Agent 编排 | 自研轻量内核（事件驱动状态机） |
| D4 | 向量库 | pgvector（初赛）；决赛可升级 Qdrant |
| D5 | 文档解析 | MinerU 本地 GPU 为主 + SiliconFlow API 配置化备胎；PaddleOCR CPU |
| D6 | 中级任务 | 初赛 5 个完整（#1+#2/#4/#5/#9）+ 3 个基础版（#3/#6/#7）+ 1 个轻量（#8）；决赛扩展到 7-8 个完整 |
| D7 | 知识库 | 自建与 Chinook 联动的虚构公司文档集（10-15 份，含 3 份故意做坏）+ OHR-Bench 作 #9 实证 |
| D8 | 高级任务 | 决赛申报“实用创新”：自研编排内核 + 检索式要素中台 + 成本工程叙事 |
| D9 | Embedding/Rerank | 本地部署（BGE-M3 + bge-reranker-v2-m3）+ API 备胎 |
| D10 | 团队/时间/预算 | 3 人 × 15-20h/周；截止 2026-10-24（实际 5 周含国庆）；API ≤ ¥1000；**跑分脚本分级（L0/L1/L2）+ W2 落地 Prompt 缓存前缀设计** |
| D11 | 前端 UI | Naive UI |
