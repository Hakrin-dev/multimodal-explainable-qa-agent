# W1 backend deps (Python 3.11+)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlglot>=25.0
psycopg[binary]>=3.1
openai>=1.30          # lazy-imported; only needed for real providers

# dev / test
pytest>=8.0
httpx>=0.27           # fastapi TestClient
